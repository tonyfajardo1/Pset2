# Trigger: magnificent illusion
# Trigger de ingesta (nombre segun evidencias)

name: magnificent illusion
trigger_type: schedule
status: active
pipeline_uuid: ingest_bronze

schedule_type: custom
schedule_interval: "@weekly"

# Variables del trigger
variables:
  service_types:
    - yellow
    - green

settings:
  allow_blocks_to_fail: false
  skip_if_previous_running: true

description: |
  Trigger de ingest_bronze restaurado con el nombre de las evidencias.
