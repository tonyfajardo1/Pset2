# Trigger: fancy warrior
# Trigger de dbt_after_ingest (nombre segun interfaz previa)

name: fancy warrior
trigger_type: schedule
status: active
pipeline_uuid: dbt_after_ingest

schedule_type: custom
schedule_interval: "@weekly"

settings:
  allow_blocks_to_fail: false

description: |
  Trigger de dbt_after_ingest restaurado para ejecutar la cadena dbt.
